on:
  push:
    branches:
      - master





jobs:
  build:
    run-on: ubuntu-latest

     steps:
      - name: 'Testing Github action'
        run: pwd
      


     - name: 'Before'
       run: ls 

     - user: actions/checkout@v3

     - name: 'After'
        run: ls 